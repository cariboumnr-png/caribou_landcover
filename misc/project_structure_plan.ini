
.
├── scripts/
│   ├── prepare_data.py            # CLI: run build/validate/split + emit manifest
│   └── train.py                   # CLI: consume manifest + configs, run training
├── configs/
│   ├── data.yaml                  # raw/processed paths, cache dirs, split params
│   ├── heads.yaml                 # head definitions & hierarchy
│   ├── training.yaml              # optim/scheduler/trainer params
│   └── curriculum.yaml            # phases sequence and rules
├── src/
│   ├── data/
│   │   ├── blocks/
│   │   │   ├── build.py           # from rasters → NPZ blocks (cache writer)
│   │   │   ├── validate.py        # sanity checks on blocks + metadata
│   │   │   ├── split.py           # train/val/test list generation
│   │   │   └── tiler.py           # ★ raster pairing + window generation utility
│   │   ├── catalog.py             # manifest (paths, versions, hashes) IO
│   │   └── transforms.py          # geometric/radiometric transforms (optional)
│   ├── models/
│   │   ├── backbones/
│   │   │   └── unet.py
│   │   ├── heads/
│   │   │   └── specs.py           # parse/validate head specs, relationships
│   │   └── multihead.py
│   ├── training/
│   │   ├── engine.py              # Trainer implementation
│   │   ├── loss.py
│   │   ├── metrics.py
│   │   ├── optim.py
│   │   ├── config.py              # dataclasses for runtime configs
│   │   └── curriculum/
│   │       ├── phases.py
│   │       └── controller.py
│   ├── pipelines/
│   │   ├── data_prep.py           # orchestrates build/validate/split → manifest
│   │   └── train.py               # wires loaders/model/trainer/curriculum
│   └── utils/
│       ├── args.py
│       ├── io.py                  # JSON/YAML read/write helpers
│       ├── logger.py
│       └── seed.py                # deterministic seeding across libs
├── tests/
│   ├── test_blocks_build.py
│   ├── test_blocks_split.py
│   ├── test_tiler.py
│   ├── test_head_specs.py
│   └── test_dataloaders.py
├── data/                          # optional local data workspace (ignored by VCS)
│   ├── raw/
│   └── cache/
├── .env                           # optional env vars
├── pyproject.toml                 # build + dependencies
└── README.md
