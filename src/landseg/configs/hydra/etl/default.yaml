# @package etl

canvas:
  reference_raster: ''
  target_crs: EPSG:3161
  target_resolution: 20.0

raw_data:
  features: {}
  domains: {}
  labels: {}

resampling_continuous: bilinear
resampling_categorical: nearest
output_dpath: ${execution.exp_root}/artifacts/harmonized
